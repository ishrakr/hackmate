Place the distributed 8th Wall engine binary files in this folder.

Expected entrypoint:

```txt
public/8thwall/xr.js
```

The AR scavenger hunt loads `/8thwall/xr.js` and preloads the `slam` chunk for
world tracking. Keep the exported engine files and any required chunk files in
this directory with their original filenames.
